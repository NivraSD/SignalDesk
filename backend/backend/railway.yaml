# Railway deployment configuration
# This file forces Railway to use server.js

build:
  builder: nixpacks

deploy:
  startCommand: node server.js
  healthcheckPath: /api/health
  restartPolicyType: always