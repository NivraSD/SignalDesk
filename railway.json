{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules && npm cache clean --force && npm ci"
  },
  "deploy": {
    "startCommand": "echo '🚀 STARTING FRESH DEPLOYMENT' && date && node app.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}